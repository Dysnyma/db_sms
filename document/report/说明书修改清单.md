# 课程设计说明书修改清单

> 基于：现有 `说明书.docx` + 项目当前代码状态
> 对标：任务书 + 说明书规范
> 目标：使说明书与当前系统功能、数据规模、技术栈完全一致

---

## 第一章 概述

### 1.1 项目背景

**当前内容**：有一段"随着高校招生规模的不断扩大……"的描述
**→ 改为**：精简为任务书原文描述风格，直接说明选题为"题目二：学生成绩管理系统"

**新增内容**（在项目背景末尾）：
```
系统采用传统人工设计方法结合 AI 辅助工具（Claude Code，路由B），
完成数据库设计的全部六个阶段，最终交付 CLI + Streamlit 双界面应用。
```

### 1.3 软硬件环境与工具

**当前内容**：技术栈表格缺失部分内容
**→ 补充为**：

| 类别 | 名称 | 版本/说明 |
|------|------|-----------|
| 数据库 | MySQL | 8.0 |
| 编程语言 | Python | 3.12 |
| CLI框架 | 标准库 + pymysql | — |
| Web框架 | Streamlit | 1.x |
| AI工具 | Claude Code | 路由B：AI增强IDE全栈辅助开发 |
| 图表库 | Plotly Express | 交互式饼图/柱状图 |
| 数据校验 | Pydantic | 输入模型验证 + 中文错误提示 |
| 连接池 | SQLAlchemy QueuePool | 5 常驻 + 10 溢出 |
| 操作系统 | Windows 11 | 10.0.26100 |
| 版本管理 | Git | — |

### 1.4 AI 工具使用清单

**1.4.1 选型路线**
**当前内容**：缺少路由说明
**→ 改为**：
```
本课程设计选择「路由B：AI增强IDE全栈辅助开发」方案，
使用 Claude Code（基于 Claude 模型）作为主要 AI 辅助工具。
```

**1.4.2 AI在各阶段应用**
**当前内容**：表格太简单
**→ 补完为**：

| 设计阶段 | AI作用 |
|----------|--------|
| 需求分析 | 阅读任务书，提取实体与业务规则，整理为结构化需求文档 |
| 概念结构设计 | 讨论实体关系建模决策（如1:N vs M:N），确认ER图设计 |
| 逻辑结构设计 | 辅助编写建表DDL，提供字段类型与约束建议，人工逐行审核修正 |
| 物理结构设计 | 分析查询模式，建议索引策略，人工使用EXPLAIN验证 |
| 数据库实施 | 生成SQL代码（视图/触发器/存储过程/函数），人工逐行审核并修复Bug |
| 应用层开发 | 编写Python CLI代码和Streamlit页面，辅助调试和重构 |
| 运行维护 | 备份恢复策略设计，排序规则修复，性能分析 |

---

## 第二章 需求分析

### 2.1 系统业务需求

**当前内容**：需求点基本齐全
**→ 补充以下内容**：

在"（3）成绩管理"中增加：
```
• 成绩录入支持逐条录入和批量 CSV 导入两种方式
• CSV 批量导入支持按行反馈结果，含中文错误提示
• 成绩录入截止时间后不能再修改（存储过程校验）
```

在"（4）统计报表"中增加：
```
• 所有统计报表要求使用存储过程实现，向应用程序传递数据
• 前端页面额外增加 Plotly 交互式图表展示（饼图/柱状图）
```

在"（6）数据维护"中增加：
```
• 班级删除时自动检查是否还有在读学生，有则拒绝删除并提示
• 学生管理列表自动过滤已删除班级的学生
• 专业列表通过 CSV 文件管理，支持增删操作
```

### 2.2 数据流图（DFD）

**当前内容**：已有数据流图，但数据量与实际差距较大
**→ 修改**：在"核心数据流描述"表格中，将各数据流的数量级改为与实际一致

| 编号 | 数据流名 | 平均流量 | 说明 |
|------|----------|----------|------|
| F1 | 选课申请 | 每学期约 2000-5000 次 | 20,000 学生规模 |
| F3 | 成绩单 | 期末约每天 500-1000 条 | 200 万选课需分批录入 |
| F6 | 排课人数更新 | 选课期间极高 | 触发器自动维护 |

### 2.3 数据字典

**2.3.1 数据项**

**当前内容**：各表字段表格齐全
**→ 修改以下字段**：

`max_students` 的取值范围从 `1~9999` **→ 改为** `1~99999`（因为 LOAD DATA 阶段设置了 99999）

**新增字段说明**（在 `course_offering` 表中）：
```
enroll_start_time / enroll_end_time / grade_deadline
这三个时间字段通过 st.datetime_input 原生组件选择，
由 _check_times 函数校验 start ≤ end ≤ deadline。
```

**2.3.4 数据存储**

**→ 修改数据量**：

| 数据存储 | 当前值 | 改为 |
|----------|--------|------|
| 班级表 | 约12条 | 约 329 条（50专业 × 3年级 × 2~3班） |
| 学生表 | 约120条 | 约 20,000 条 |
| 教师表 | 约16条 | 约 300 条 |
| 排课表 | 约40条 | 约 1,004 条 |
| 选课表 | 每学期约200-500条 | 约 2,002,688 条 |

**新增数据存储：备份文件**
```
| 数据存储名 | 备份文件 |
| 数据量 | 约 50-80MB（含200万选课数据） |
| 说明 | 通过 mysqldump --routines --triggers 导出 |
```

---

## 第三章 概念结构设计

### 3.1 全局 ER 图

**当前内容**：实体清单、关系清单、关键决策都有
**→ 补充**：

在实体描述中增加 `major_manage_page` 相关说明（专业管理虽不在DB实体中，但作为前端功能增加了专业管理模块，数据存储在 CSV 文件中）。

**→ 截图位置**：在此处插入「总体ER图.png」

### 3.2 AI 辅助概念设计记录

**当前内容**：已有
**→ 补充**：
```
• 专业管理模块：AI 建议将专业列表以 CSV 形式存储（而非数据库表），
  方便老师直接在文件管理器中编辑。经讨论采纳。
• 班级名自动生成策略：AI 提出按 {年级}{专业}{序号}班 格式自动生成，
  人工确认后实现。
```

---

## 第四章 逻辑结构设计

### 4.1 基础数据表

**当前内容**：8 张表的 DDL 都在
**→ 需要修改的地方**：

| 表 | 字段 | 修改 |
|----|------|------|
| `class` | grade | 类型改为 `VARCHAR(10)`（与现有代码对齐） |
| `student` | weighted_score | 补充说明：由触发器 `trg_enrollment_after_insert_score` 和 `trg_enrollment_after_update_score` 自动维护 |
| `teacher` | title | 补充说明：前端已改为 selectbox 下拉选择（教授/副教授/讲师/助教等） |
| `course_offering` | max_students | 补充说明：前端 text_input + max_chars=5 限制取值范围 |
| 全部表 | status | 补充说明：`status` 管业务状态，`is_deleted` 管数据状态，两者分开 |

**新增一段说明**（在 4.1 末尾）：
```
### 专业列表管理
专业列表不存储在数据库表中，而是以 CSV 文件 `data/majors.csv` 管理，
通过 `src/core/majors.py` 模块提供 `all_majors()` / `add_major()` / `delete_major()` 接口。
前端在「数据管理 → 专业管理」页面提供图形化增删操作。
当前共 160+ 个专业，涵盖工、理、医、文、法、经、管、艺、体等学科门类。
```

### 4.3 AI 辅助逻辑设计修正对照表

**→ 补充新条目**：

| 字段/表名 | AI原始方案 | 人工修正后 | 修正理由 |
|-----------|-----------|-----------|----------|
| 专业列表存储方式 | 数据库表 | CSV 文件 | 便于老师直接在文件管理器中编辑，无需数据库操作 |
| 班级名生成 | 用户手动输入 | 自动生成 {年级}{专业}{序号}班 | 减少输入错误，格式统一 |
| 教师工号格式 | 纯数字 | T 开头 + 数字（如T20240001） | 与学号格式区分，便于识别角色 |
| 登录输入 | 直接查库 | 先格式校验（区分 admin/T开头/纯数字） | 避免无效查询，提升体验 |
| 教师职称 | text_input | selectbox 下拉（教授/副教授/讲师等） | 标准化职称输入，避免手写不一致 |
| 排课时间选择 | date_input+时/分下拉 | st.datetime_input 原生组件 | 砍掉 108 行代码，交互更流畅 |
| 表单输入校验 | 无 | Pydantic 模型 + max_chars | 前端后端双重校验，防止脏数据 |

### 4.4 约束与完整性说明

**→ 补充说明**：

```
### 前端输入校验约束
除数据库层面的约束外，本项目在应用层增加了 Pydantic 输入校验模型：
• 所有 CRUD 表单使用 validate_or_error 统一校验
• 校验失败时通过 st.toast 显示中文错误提示
• text_input 全部配置 max_chars 参数，与后端模型长度限制对齐
• 登录输入区分三种格式：admin / 教师工号(T+数字) / 学生学号(纯数字8-12位)

### 物理删除保护
• 班级删除时检查 COUNT(*) FROM student WHERE class_id=？AND is_deleted=0
  有在读学生则拒绝删除并提示
• student_full_list 查询增加 AND c.is_deleted=0 过滤已删除班级的学生
```

---

## 第五章 物理设计

### 5.1 存储结构

**当前内容**：已有 InnoDB/utf8mb4/主键策略说明
**→ 补充**：
```
### 连接池配置
系统通过 SQLAlchemy QueuePool 管理数据库连接：
• pool_size = 5（常驻空闲连接数）
• max_overflow = 10（峰值额外临时连接数）
• pool_pre_ping = True（取连接前自动检测，失效自动重连）
• pool_recycle = 3600（1小时强制回收连接）
```

### 5.2 索引设计

**→ 补充索引**：

| 表名 | 索引字段 | 类型 | 理由 |
|------|----------|------|------|
| course_offering | (enroll_start_time, enroll_end_time) | 复合索引 | 选课时间范围查询 |
| course_offering | teacher_id | 普通索引 | 教师查看排课（高频） |
| enrollment | student_id | 普通索引 | 按学生查成绩（极高频率） |
| teacher_course | (teacher_id, course_id) | 唯一索引 | 防重复讲师关系 |

### 5.2 AI 辅助物理设计记录

**→ 截图位置**：在此处插入「EXPLAIN执行计划截图.png」
**→ 补充 EXPLAIN 示例**：
```
-- 查询：某学生已选课程（极高频）
EXPLAIN SELECT * FROM enrollment WHERE student_id = 5 AND is_deleted = 0;
-- type=ref, key=idx_enr_student, rows≈8

-- 查询：按教师查排课（高频）
EXPLAIN SELECT * FROM course_offering WHERE teacher_id = 3 AND is_deleted = 0;
-- type=ref, key=idx_co_teacher, rows≈5
```

---

## 第六章 数据库实施

### 6.1.2 初始化数据

**当前内容**：数据量严重过时（12班/120学生/12课程/16教师）
**→ 全面替换为**：

| CSV文件 | 内容 | 数据量 |
|---------|------|--------|
| class.csv | 班级信息 | 24条（初始） |
| student.csv | 学生信息 | 1,000条（初始） |
| teacher.csv | 教师信息 | 25条（初始） |
| course.csv | 课程信息 | 18条 |
| teacher_course.csv | 讲授关系 | 约60条 |
| course_offering.csv | 排课安排 | 54条（初始） |
| enrollment.csv | 选课+成绩 | 约3,000条（初始） |

**新增一节**（在6.1.2之后）：
```
### 6.1.3 大数据量测试数据生成
为模拟真实运行环境，项目提供大数据量生成器 `test/generate_large_data.py`：

| 实体 | 基础数据 | 扩增后 |
|------|----------|--------|
| 学生 | 1,000 | 20,000 |
| 教师 | 25 | 300 |
| 班级 | 24 | 329 |
| 排课 | 54 | 1,004 |
| 选课 | ~3,000 | 2,002,688 |

生成策略：
1. 使用 Faker 库生成仿真姓名，流式写入 CSV
2. LOAD DATA LOCAL INFILE 极速导入（比 INSERT 快 20×）
3. 导入前预删除触发器，导入后手动批量重算 GPA
4. 全部流程自动化，约 30 秒完成
```

### 6.2 视图、存储过程、触发器、函数实现

**→ 修改存储过程数量**：从 8 个 → **9 个**（增加了 `sp_show_courses`）

存储过程表格更新：

| 编号 | 存储过程 | 参数 | 功能 | 特殊逻辑 |
|------|----------|------|------|----------|
| SP1 | sp_show_courses | p_student_no | 查询可选课程 | 过滤已选同课程的所有教师、过滤选课期外、过滤满员 |
| SP2 | sp_enroll | p_student_no, p_plan_id | 选课 | 4步校验+事务+FOR UPDATE行锁 |
| SP3 | sp_unenroll | p_student_no, p_plan_id | 退选 | 校验截止时间 |
| SP4 | sp_grade_input | p_teacher_no, p_plan_id, p_student_no, p_score | 录入成绩 | 三重身份校验+时间校验+成绩范围 |
| SP5 | sp_student_roster | p_class_id | 班级名单 | 按学号排序输出 |
| SP6 | sp_class_grade_report | p_class_id, p_course_id | 班级成绩统计 | 均分/最高/最低/及格率 |
| SP7 | sp_student_semester_avg | p_student_no, p_semester | 学期均分 | GROUP BY 聚合 |
| SP8 | sp_teacher_info | p_teacher_no | 教师统计 | LEFT JOIN |
| SP9 | sp_teacher_list | 无 | 全部教师统计 | 按排课数降序 |

### 6.3 AI 辅助编码与人工审核记录

**→ 添加以下修正条目**：

| 修正类型 | 问题 | 修正 | 影响 |
|----------|------|------|------|
| Bug | `trg_enrollment_before_insert` 未使用 FOR UPDATE | 加 FOR UPDATE 行级锁 | 修复并发选课超卖问题 |
| Bug | teacher_tui.py 排课查询缺失 `c.is_deleted=0` | 补充过滤 | 已删除课程不再显示 |
| Bug | `import_data.py` 使用 utf-8 而非 utf-8-sig | 改为统一 utf-8-sig | 兼容 Windows Excel BOM |
| Bug | `_make_page` 异常时未 rollback | 新增 except + conn.rollback() | 防止未决事务污染连接池 |
| Bug | `all_majors()` 文件不存在时崩溃 | 自动创建空文件 | 红屏修复 |
| Bug | `grade_roster_data` 用字符串"未录入"而非 NULL | 改为返回原生 NULL | 统一数据类型 |
| Bug | 成绩分布饼图包含 0% 扇区 | 过滤 v > 0 | 修复标签显示异常 |
| Bug | 班级成绩统计除零错误 | 过滤空 scores 列表 | ZeroDivisionError 修复 |
| 优化 | 登录快速选择读取 CSV | 改为查数据库 + `@st.cache_data` 缓存 | 消除文件依赖，减少重绘查询 |
| 优化 | CSV 路径含 test/ 前缀 | 配合 cwd=TEST_DIR 去掉前缀 | LOAD DATA 路径正确 |
| 优化 | GPA 计算逐行触发 | 删触发器 → LOAD → 手动批量 UPDATE | 200 万选课只需 30 秒 |

---

## 第七章 系统实现与测试

### 7.1 系统架构

**当前内容**：项目目录结构老旧，缺少 pages/ 和 test/ 目录
**→ 替换为**：

```
db_lab/
├── src/                    # Python 代码
│   ├── app.py              # Streamlit Web 入口（22个页面导航）
│   ├── main.py             # CLI 入口
│   ├── admin.py            # 教务功能（CRUD + 统计查询）
│   ├── student_tui.py      # 学生功能（CLI）
│   ├── teacher_tui.py      # 教师功能（CLI）
│   ├── tester.py           # 测试员跳板
│   ├── import_data.py      # CSV 数据导入
│   ├── reset_data.py       # 一键重建数据库
│   ├── pages/              # Streamlit 页面模块
│   │   ├── admin_page.py   # 教务 14 页面（含 Plotly 图表）
│   │   ├── teacher.py      # 教师 3 页面
│   │   └── student.py      # 学生 5 页面
│   └── core/
│       ├── config.py       # 数据库连接池（SQLAlchemy QueuePool）
│       ├── auth.py         # 登录认证
│       ├── models.py       # Pydantic 输入校验模型
│       ├── majors.py       # 专业列表管理
│       └── utils.py        # 工具函数
├── sql/                    # MySQL 脚本（7个文件）
├── data/                   # CSV 数据
│   ├── generate_data.py    # 基础数据生成器
│   ├── majors.csv          # 专业列表（160+专业）
│   └── *.csv               # 7表初始化数据
├── test/                   # 大数据量测试
│   ├── generate_large_data.py  # 200万数据生成器（一键全自动）
│   └── load_large_data.sql     # LOAD DATA 导入脚本
├── backup/                 # 数据库备份（mysqldump）
└── document/               # 课程设计文档、报告、PPT
```

**代码量统计更新**：

| 文件 | 行数 | 说明 |
|------|------|------|
| src/pages/admin_page.py | ~1,280 | 教务14页面 + Plotly图表 |
| src/app.py | ~750 | Streamlit主入口 + 22页面路由 |
| src/admin.py | ~1,300 | 教务查询 + CRUD |
| src/pages/teacher.py | ~160 | 教师3页面 |
| src/teacher_tui.py | ~280 | 教师CLI |
| src/pages/student.py | ~80 | 学生5页面 |
| src/student_tui.py | ~170 | 学生CLI |
| src/core/models.py | ~200 | Pydantic校验模型 |
| src/core/majors.py | ~50 | 专业列表管理 |
| test/generate_large_data.py | ~200 | 大数据生成器 |

### 7.2 功能模块

**→ 全面替换功能描述**：

**学生端（5功能）**：

| 功能 | 实现 |
|------|------|
| 查询可选课程 | sp_show_courses → st.dataframe 表格 |
| 选课 | selectbox选择排课 + 按钮确认 |
| 退选 | selectbox选择排课 + 按钮确认 |
| 查看我的成绩 | st.dataframe + Plotly柱状图（各科成绩，带60分及格线） + st.metric学籍分/绩点分 |
| 学期均分 | selectbox下拉选择学期（从数据库读取该学生有数据的学期） |

**教师端（3功能）**：

| 功能 | 实现 |
|------|------|
| 录入成绩 | selectbox选排课 → text_input学号+成绩 → 按钮提交（Pydantic校验） |
| 批量CSV录入 | st.file_uploader上传 → 逐行 sp_grade_input → 按行反馈（中文错误提示） |
| 查看课程学生 | selectbox选排课 → dataframe + 成绩分布饼图 + 成绩排行柱状图（左右并排） + 未录入清单（折叠展示） + 统计指标（平均分/最高/最低/已录入） |

**教务端（16功能）**：

**查询统计类（7个）：**
- **数据概览**：6个 metric 总量卡片 + 学生/班级/教师状态饼图（3个）+ 各专业班级数柱状图 + 各年级学生数柱状图
- **班级学生名单**：selectbox选班级 → dataframe + 学籍分分布饼图 + 绩点分分布饼图 + 班级均分
- **班级成绩统计**：selectbox选班级+课程 → 5个 metric（均分/最高/最低/及格率/人数） + 成绩分布柱状图（优秀/良好/中等/及格/不及格）
- **班级成绩明细**：selectbox选班级 → dataframe（每人每课成绩） + 各课程平均分柱状图 + 课程统计表格
- **教师信息**：text_input输入工号 → 4个 metric
- **教师列表**：dataframe + 排课数排行柱状图 + 选课学生数排行柱状图
- **专业管理**：dataframe列表 + text_input新增 + selectbox删除（检查班级引用）

**数据管理类（6个）**：
- 班级管理：年级+专业 selectbox → 自动生成班级名 → INSERT | 修改只改状态（班级名/年级/专业只读） | 删除检查学生数
- 课程管理：text_input课程名 + 学分（max_chars=4） | 修改/删除
- 教师管理：姓名/工号/职称（selectbox下拉）/电话 | 修改/删除
- 学生管理：姓名/学号（Pydantic校验8-12位纯数字） | 修改/删除
- 排课管理：课程+教师+学期selectbox + 上限（text_input + max_chars=5） + datetime_input选3个时间
- 选课管理：学期 selectbox → 输入学号查询该生选课 → text_input退选ID → 强制退选

**系统工具类（2个）**：
- 备份数据：mysqldump --routines --triggers --add-drop-table
- 恢复数据：mysql < file + FOREIGN_KEY_CHECKS=0

**→ 截图位置**（插入以下页面截图）：
1. 数据概览页面（含饼图和柱状图）
2. 班级管理页面（年级/专业下拉 + 自动生成班级名）
3. 教师查看课程学生页面（饼图+排行并排）
4. 学生我的成绩页面（柱状图+及格线）
5. 排课管理页面（datetime_input 组件）
6. 登录页面（快速选择下拉）
7. 教师列表页面（排课数排行）

### 7.3 测试结果

**当前内容**：数据量严重过时（12班/120学生/16教师/40排课/200选课）
**→ 全面替换为**：

#### 功能测试

- ✅ CRUD 操作全部正常，逻辑删除后列表不显示
- ✅ 选课名额满员抛出异常并提示具体人数
- ✅ 同一门课不能选两个不同教师的班级
- ✅ 选课/退选后 current_students 实时更新
- ✅ 成绩录入后触发器自动计算学籍分和绩点分
- ✅ 成绩修改后触发器自动重算

#### 输入校验测试

- ✅ 全部表单使用 Pydantic 模型校验，中文 toast 错误提示
- ✅ text_input 配置 max_chars 对齐模型长度限制
- ✅ 登录输入区分三种格式（admin/T开头+数字/纯数字8-12位）
- ✅ CSV 批量导入错误信息中文显示

#### 统计报表测试

- ✅ 饼图展示成绩分布（不使用 0% 扇区）
- ✅ 柱状图展示排名和对比
- ✅ 图表交互式（悬停显示明细、缩放）

#### 初始化数据规模

| 实体 | 基础数据 | 测试规模 |
|------|----------|----------|
| 班级 | 24 | 329 |
| 学生 | 1,000 | 20,000 |
| 课程 | 18 | 18 |
| 教师 | 25 | 300 |
| 排课 | 54 | 1,004 |
| 选课记录 | ~3,000（含~25%未录入） | 2,002,688 |

---

## 第八章 系统运行与维护

### 8.1 备份与恢复

**当前内容**：已有备份恢复说明
**→ 补充**：
```
### 大数据量备份注意事项
当数据库包含 200 万条选课记录时，mysqldump 导出文件约 50-80MB。
建议在系统低负载时段执行备份。备份命令需包含 --routines --triggers
参数以确保完整导出所有数据库对象。
```

### 8.3 AI 辅助测试数据生成

**→ 新增一节**（将原有8.3替换为）：
```
### 8.3 AI 辅助测试数据生成

系统提供两套测试数据生成方案：

1. 基础数据生成（data/generate_data.py）
   • 生成 1,000 学生、25 教师、24 班级、18 课程、54 排课、~3,000 选课
   • 约 25% 选课不生成成绩（模拟未录入场景）
   • 数据覆盖 3 个年级 × 6 个专业

2. 大规模数据生成（test/generate_large_data.py）
   • 一键生成 20,000 学生、300 教师、329 班级、1,004 排课、200 万选课
   • 使用 Faker 生成仿真姓名，流式写入 CSV
   • LOAD DATA LOCAL INFILE 极速导入
   • 导入前预删除触发器，导入后手动批量重算 GPA
   • 整体用时约 30 秒

边界值测试场景：
• 选课满员（max_students=30, current=30 时选课被拦）
• 截止时间后操作被拒绝
• 空值处理（学号不存在、工号不存在）
• 非法输入（成绩 > 100 或 < 0）
• 超大数字（排课上限输入超过 max_chars=5 被截断）
```

---

## 第九章 总结

### 9.1 课程设计成果

**当前内容**：数据已过时
**→ 改为**：

| 成果 | 详情 |
|------|------|
| 数据库设计 | 8张表、3个视图、5个触发器、9个存储过程、2个函数 |
| CLI应用 | 4角色命令行界面 |
| Web应用 | 22个Streamlit页面，含Plotly交互式图表 |
| ER图 | Draw.io绘制 |
| 测试数据 | 20,000学生/300教师/329班级/200万选课记录 |
| 代码量 | Python ~5,000行，SQL ~500行 |
| AI辅助 | Claude Code全流程辅助，含详细使用记录 |
| 版本管理 | Git，80+次提交 |

### 9.1 AI 工具使用效果与反思

**→ 补充**：

#### 9.1.1 效率分析

**省时方面**：
- DDL/SQL 代码生成：节省约 70%
- 重复性 CRUD 代码：节省约 80%
- Bug 排查：快速定位排序规则冲突、SQL注入风险、缩进错误
- 文档生成：说明书初稿由 AI 生成后人工修改

**费时方面**：
- Streamlit 组件行为（datetime_input/format 参数）调试
- 大规槰数据生成策略迭代（CROSS JOIN → LOAD DATA → 删触发器）

#### 9.1.2 AI 亮点与局限

**亮点**：
- 能快速理解复杂业务需求，生成结构正确的 SQL 和 Python 框架
- 重构和调试时提供多种备选方案
- 对 MySQL 语法细节把握准确（FOR UPDATE 行锁、DELIMITER 等）

**局限**：
- 对 Streamlit 组件行为理解不够深入（datetime_input format 限制）
- 大数据量场景经验不足（CROSS JOIN 3次产生 540 亿行中间结果）
- 某些框架细节（st.form/st.tabs）需要人工验证

---

## 附录

### 附录A：核心SQL代码

无需修改，但建议在代码附录中统一引用 `代码附录.docx`

### 附录中需要插入的截图汇总

| 章节 | 截图内容 | 说明 |
|------|----------|------|
| 3.1 | 总体ER图.png | Draw.io 导出的完整 ER 图 |
| 5.2.2 | EXPLAIN执行计划.png | 关键查询的 EXPLAIN 分析结果 |
| 7.2 | 数据概览.png | 饼图+柱状图+metric 卡片 |
| 7.2 | 班级管理.png | 年级专业下拉+自动生成班级名 |
| 7.2 | 教师查看课程学生.png | 饼图+排行并排 |
| 7.2 | 学生我的成绩.png | 各科柱状图+及格线 |
| 7.2 | 排课管理.png | datetime_input 组件 |
| 7.2 | 登录页面.png | 快速选择下拉 |
| 7.2 | 教师列表.png | 排课数排行柱状图 |
| 7.2 | 数据规模验证.png | 200万选课导入结果截图 |

---

## 修改优先级建议

| 优先级 | 内容 | 工作量 |
|--------|------|--------|
| 🔴 高 | 各章节数据量更新（关键数据严重过时） | 调整10+处数字 |
| 🔴 高 | 第七章系统架构更新（目录结构+代码量） | 替换2段 |
| 🔴 高 | 第六章初始化数据规模更新（6.1.2） | 替换1段 |
| 🟡 中 | 第一章技术栈补充（Plotly/Pydantic/SQLAlchemy） | 补充表格 |
| 🟡 中 | 第四章AI修正对照表新增条目 | 补充表格 |
| 🟡 中 | 第八章新增8.3大数据生成 | 新增1节 |
| 🟢 低 | 各截图插入 | 10张截图 |
| 🟢 低 | 附录AI对话记录补充 | 可选 |
